# ----------------------------------------------------------------------
# Lone Wolf Dumpsters - Namecheap cPanel Apache Configuration (.htaccess)
# ----------------------------------------------------------------------

# Set DirectoryIndex to prefer PHP over HTML in admin
DirectoryIndex index.php index.html

# 0. Crawler Protection Rules (Host & Admin Scoped)
<IfModule mod_headers.c>
  # Enforce noindex, nofollow strictly on test/staging domain
  SetEnvIf Host "^test\.lonewolfdumpsters\.com$" IS_TEST_DOMAIN
  Header set X-Robots-Tag "noindex, nofollow" env=IS_TEST_DOMAIN

  # Enforce noindex, nofollow on admin management routes
  SetEnvIf Request_URI "^/admin" IS_ADMIN_ROUTE
  Header set X-Robots-Tag "noindex, nofollow" env=IS_ADMIN_ROUTE
</IfModule>

# 1. Force HTTPS
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  # Redirect unauthenticated static /admin/index.html access directly to /admin/
  RewriteRule ^admin/index\.html$ /admin/ [R=301,L]
</IfModule>

# 2. Canonical Hostname (Remove www -> Non-www)
<IfModule mod_rewrite.c>
  RewriteCond %{HTTP_HOST} ^www\.lonewolfdumpsters\.com [NC]
  RewriteRule ^(.*)$ https://lonewolfdumpsters.com/$1 [L,R=301]
</IfModule>

# 3. Custom Error Document (404 Page)
ErrorDocument 404 /404.html

# 4. Prevent Directory Browsing
Options -Indexes

# 5. Protect Sensitive Logs & Data Files from Direct Public Downloads
<FilesMatch "^(leads\.log|site-content\.json|config\.php|\.env|config\.ini|\.git)">
  Order allow,deny
  Deny from all
</FilesMatch>

# 6. Enable GZIP Compression (mod_deflate)
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/plain
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/xml
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE application/xml
  AddOutputFilterByType DEFLATE application/xhtml+xml
  AddOutputFilterByType DEFLATE application/rss+xml
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/x-javascript
  AddOutputFilterByType DEFLATE application/json
  AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>

# 7. Browser Caching Headers (mod_expires)
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresDefault "access plus 1 month"

  # HTML - short cache for instant updates
  ExpiresByType text/html "access plus 0 seconds"
  ExpiresByType application/json "access plus 0 seconds"

  # CSS & JavaScript
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType application/x-javascript "access plus 1 year"

  # Images & Fonts
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
  ExpiresByType application/font-woff2 "access plus 1 year"
</IfModule>

# 8. Security Headers
<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set X-XSS-Protection "1; mode=block"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>
